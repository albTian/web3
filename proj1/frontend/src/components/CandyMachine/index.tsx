import {
  candyMachineProgram,
  TOKEN_METADATA_PROGRAM_ID,
  SPL_ASSOCIATED_TOKEN_ACCOUNT_PROGRAM_ID,
} from "../../api/helpers";
import { programs } from "@metaplex/js";

// const {
//   metadata: { Metadata, MetadataProgram },
// } = programs;
const CandyMachine = () => {
  return <div>dogwater</div>;
};

export default CandyMachine;
